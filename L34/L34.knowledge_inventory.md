# L34 知识清单

## 来源与边界

- 主来源：`L34_final_working.md`，时长 `01:51:25`，223 个 30 秒合并窗口。
- 补充来源：`L34/doc/阅读 8 批改.学习笔记.pdf`（**学生本人的课堂笔记**，`compréhension
  écrite test 8 (Série 151)`，用于确认题号与题面）。**老师的笔记版尚未提供**，卷面上的
  划线、高亮与手写一律是学生自己的，不构成“老师要求背”的证据；该判定只以转录为准。
- **不作为词表来源**：`L34/doc/1.xlsx`。转录 `01:16:00` 显示这是学生**已有的**
  词表记事本（其中一段正是 L33 的 12 个词），老师当堂看过并确认格式可用。它的
  角色是"背单词方法"这一知识点的证据，不整份倒入 L34 词汇。
- 提取方式：一次有边界的综合通读加候选证据定点复核；不使用完整度百分比或统计
  饱和门。
- 分流规则：裸词义和固定表达进入 `vocab.json`；用法、选择、纠错、应试与写作
  策略进入知识 deck。
- 复用模式：沿用 L33 的稳定 `species_label -> card_overrides / checkpoint_groups
  -> build_checkpoints_from_species.py` 路径，不手改 `manifest.json`。

## 课程主线

1. **开课听写与词义边界**（`00:00-20:00`）：24 个听写词逐个核对；第二组动词
   `finir` 复数变位纠错；`métier/atelier`、`réveillon`、`tarif/gratuit`、
   `afficher/affiche`、`se rencontrer/se connaître` 五组边界。
2. **阅读词汇陷阱与构词**（`22:00-01:10:00`）：`facile→faciliter`、`capacité`、
   `physique` 不含脑力、`rédaction/droit` 英法形近词、`re-` 前缀与 `d'occasion`、
   `gros/grosse` 阴性。
3. **阅读定位与选项判断**：先题干再选项后正文的定位顺序；拒绝合理外推；选项须
   覆盖全文；广告末句才是诉求；角色转换；上位词包容；背词范围限于题干、四个
   选项与定位句。
4. **写作 T1**（`01:18:00-01:29:00`）：`Je ne sais pas trop comment m'y prendre`
   求助句；形容词不能悬空；80–100 词；不交 AI 改写版；CLB 7 四条标准。
5. **写作 T2**（`01:29:00-01:51:25`）：标题→引入→经历→推荐四段结构；感受式与
   记叙式两种正文写法；列举连接词必备；三个万能句式；blog 与 courriel 两种格式。

## 8501 checkpoint 分组

| 顺序 | 分组 | Species | 额外练习卡 |
|---:|---|---:|---:|
| 10 | 听写词义边界与变位纠错 | 6 | 0 |
| 20 | 阅读词汇陷阱与构词 | 6 | 0 |
| 30 | 阅读定位与选项判断策略 | 10 | 0 |
| 40 | 写作 T1：批改点与备考规范 | 6 | 1 |
| 50 | 写作 T2：结构、连接与万能句 | 5 | 1 |

## 词汇路由

- `vocab.json`：**79 条**（上线基线 50 条 + T8 ingest 增补 29 条），全部带结构化
  `provenance`。构成：**老师要求 57 条**（中文义只带题号前缀，无后缀）+ **Opus5 补充
  22 条**（带 `[Opus5 建议：…]` 后缀）。前缀分布：`[T8Qn]` 系列、`[L34课前复习]` 22、
  `[L34课外题]` 2、`[L34写作T1]` 1、`[L34写作T2]` 4。直接引用转录的证据，其时间段与
  行号一一配对且行号范围包含对应时间戳头。
- 增补明细与判定规则见 `L34.screenshot-vocab-review.md`；T8 题面与卷面标记见
  `L34.T8.reading-evidence.md`。
- `L34.card_overrides.json`：33 个 reviewed species 全部精修，无通用模板。
- `L34.checkpoint_groups.json`：33 个 species 恰好分配一次，另补 2 张可机判的
  应用练习卡。
- `manifest.json`：由原生 builder 生成 35 张知识点卡，其中 34 张机判、1 张自评。

## 教师明确的记忆边界（不得升级为记忆任务）

- 可以不用记：`climatiseur`、`vitre`、`auto radio`、`air conditionné`
  （`27:00-27:30` 原话"其他的单词……你可以不用记，因为阅读的单词有点多了一点"）。
- 有点印象即可：`humanitaire`（`46:30-47:00`，"考过口语但特别偏"）。
- 只识别不产出：虚拟式（`01:24:00-01:25:00`，"T1 不特别要求，放到 T3 再讲"）。

## 已知证据缺口

- `la retraite`、`un départ` 出现在开课听写里，但教师核对其中文释义的片段被 ASR
  破坏，释义不可恢复，故未收录，也未猜测。
- `00:30-01:00` 窗口三路合并选错来源（VibeVoice 幻觉），已列为待核对第一项。
- `une brocante`、`Je ne sais pas trop comment m'y prendre` 的拼写由教师中文解释
  反推，已在词条 `learning_note` 标注。
- 阅读 test 8 的 PDF 含 Q1–Q22，转录中可确认逐题讲解的是其中一部分；未见讲解的
  题号只作为 Opus5 补充词来源，不冒充老师要求。
- T8 ingest 只完成 B1（Q1–Q11）；B2（Q12–Q22）待办。
- 转录 `01:12:30` 老师说“把我挑选的这些单词背一下”，指他自己标黄的词；老师笔记版
  到手前这批词无法解析。
- 未创建写作任务：T2 方法论完整但课堂未给出完整作文题面（教师说课后另发），
  证据不足，列为后续候选。
- 待核对清单见 VibeVoice 仓库 `docker-data/outputs/L34/L34_uncertain_windows.md`
  （223 窗口中 30 个低可信，已列出最需确认的 20 个）。拿到答复后按增量维护补录，
  不需要重跑整课提取。

## 构建约束

- `manifest.json` 必须由 `scripts/build_checkpoints_from_species.py` 生成。
- `species_label` 是 source、override 和 group 的稳定连接键。
- 生成后必须通过 coverage report、AppTest 可见性测试和真实 8501 健康检查。
