# L33 知识清单

## 来源与边界

- 主来源：`L33_final_working.md`，时长 `01:52:02`。
- 提取方式：一次有边界的综合通读，加候选证据定点复核；不使用完整度百分比或统计饱和门。
- 分流规则：裸词义和固定表达进入 `vocab.json`；用法、选择、纠错、代词接口与学习策略进入知识 deck。
- 复用模式：沿用 L32 的稳定 `species_label -> card_overrides / checkpoint_groups -> build_checkpoints_from_species.py` 路径，不手改 `manifest.json`。

## 课程主线

1. **词义与词形边界**：`la poste / le poste`、`passer une commande`、`concours` 单复同形。
2. **写作逻辑与精简**：原因不用 `mais`、合并重复住房信息、`aider quelqu'un à + infinitif`、条件式只在可控时使用。
3. **代词与口语识别**：`Je le trouve intéressant`、`Ma sœur me l'a offert`、`t'as l'air` 的识别/产出边界。
4. **学习策略**：代词块通过书写和重读固化；1–20 用于精听复述，21–39 留到考前分级背诵。

## 8501 checkpoint 分组

| 顺序 | 分组 | Species | 额外练习卡 |
|---:|---|---:|---:|
| 10 | 词义、搭配与词形边界 | 3 | 0 |
| 20 | 写作逻辑、宾语接口与可控表达 | 4 | 0 |
| 30 | 代词顺序与口语识别 | 4 | 0 |
| 40 | 分阶段听阅与考试策略 | 2 | 1 |

## 词汇路由

- `vocab.json`：12 个可靠课堂词汇和表达，全部带结构化来源。
- `L33.card_overrides.json`：13 个 reviewed species 全部精修，其中 10 张机判、3 张自评。
- `L33.checkpoint_groups.json`：13 个 species 恰好分配一次，并补 1 张可机判的阶段策略应用题。
- `manifest.json`：由原生 builder 生成 14 张知识点卡，其中 11 张机判、3 张自评。

## 已知证据缺口

- 开场 `00:30-04:30` 和 `16:30-18:30` 有多处 ASR 畸变；无法稳定恢复的词没有为凑数量而收录。
- `01:44:30-01:45:30` 的部分代词例句拼写不可恢复，因此只保留老师明确讲出的学习策略和可核实句型。
- 后续若从原音频定点校音出新材料，应作为增量维护加入，不需要重跑整课提取。

## 构建约束

- `manifest.json` 必须由 `scripts/build_checkpoints_from_species.py` 生成。
- `species_label` 是 source、override 和 group 的稳定连接键。
- 生成后必须通过 coverage report、AppTest 可见性测试和真实 8501 健康检查。
